Briaton – Интернет-магазин светильников

🚀 React + TypeScript интернет-магазин с корзиной, фильтрацией и анимациями.

🔗 Демо: [] (еще не добавил)
📂 Исходный код: https://github.com/Ssatanaxxx/Briaton

ВАЖНО: ПРОЕКТ ЯВЛЯЕТСЯ ЛИШЬ ПЕРЕРАБОТАННЫМ ПРОЕКТОМ НАПИСАННЫМ НА ВАНИЛЬНОМ КОДЕ (Html,css,js)
ПРОЕКТ НЕ ГОТОВЫЙ: НЕ ИСПРАВЛЕНО КНОПКА "ВОЙТИ", "КОРЗИНА", "РЕНДЕР ВЫВЕДИННЫХ ПРОДУКТОВ"

🔍 О проекте
Briaton – это SPA-магазин светильников с:
✅ Каталогом товаров (сортировка, фильтрация)
✅ Корзиной (добавление/удаление, подсчёт стоимости)
✅ Адаптивным дизайном (но требует доработок на mobile)
✅ Анимациями (плавные переходы, модальные окна)
✅ Контекстом для состояния (BasketContext)

🛠 Технологии
React 18 (хуки, lazy loading)

TypeScript (типизация компонентов, API-ответов)
SCSS (модульные стили, анимации)
React Router v6 (навигация между страницами)
Vite (быстрая сборка)

✨ Что реализовано хорошо
1. Чистая архитектура
📁 Логичное разделение на:

components/ – переиспользуемые UI-элементы (ProductCard, Modal)

pages/ – страницы приложения (Home, Catalog, Basket)

context/ – управление состоянием корзины (BasketContext)

2. Работа с API
🌐 Получение товаров через fetch с обработкой загрузки:

tsx
const [products, setProducts] = useState<Product[]>([]);
const [isLoading, setIsLoading] = useState(true);
3. TypeScript
🔒 Типизированы:

Пропсы компонентов (interface ProductCardProps)

Данные корзины (BasketItem)

API-ответы

4. Динамический UI
🎨 Применены:

CSS-анимации (наведение, модалки)

Ленивая загрузка страниц (React.lazy)

Адаптивная сетка (grid/flex)

5. Корзина
🛒 Полноценный функционал:

Добавление/удаление товаров

Подсчёт общей суммы

Сохранение в localStorage (можно добавить)

🚀 Запуск проекта
Установка зависимостей:

bash
npm install
Запуск dev-сервера:

bash
npm run dev
Сборка для продакшена:

bash
npm run build
